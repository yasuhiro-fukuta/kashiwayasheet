# 引き継ぎ：柏屋 予約同期システム

別スレッドで、このスプレッドシートを読んでスタッフの質問に答えてもらうための引き継ぎ文書。
**このファイルをそのまま新しいスレッドに貼れば、前提が伝わるように書いてある。**

---

## 1. これは何か

長野の旅館「柏屋」（客室2つ：**1F**＝和室 / **2F**＝ファミリールーム）の予約・清掃管理。
Google スプレッドシート `kashiwaya_master_v2` を台帳に、Apps Script が**毎時**動いて、
Booking.com / Airbnb の iCal、Lodgify API、Google フォーム2種から情報を集めて
**清掃予定表（CleaningBoard）**と**食事予約表（LatestOptions）**を作っている。

新スレッドでやりたいのは、**スタッフの3つの質問に答えること**（§5）。
システムを直す話ではない。

---

## 2. シート一覧

| シート | 中身 | 誰が書くか |
|---|---|---|
| **CleaningBoard** | 日付×部屋の清掃・接客予定。**主役** | A〜D・U・V＝人 / E〜T＝バッチ |
| **特** | 特別清掃のタスク表 | 人（バッチは読むだけ） |
| **Staff** | 担当者一覧 | バッチが追記 / 表示名は人が編集 |
| LatestOptions | 食事予約表（フォーム由来 **＋ Lodgify 予約時オプション**） | バッチ |
| FormResponses | 食事・オプション注文フォームの生回答 | フォーム |
| LodgifyBookings | Lodgify API の取得結果（人数の出どころ。T列に予約時オプション） | バッチ |
| LatestReservations | iCal 由来の在室（1泊1行） | バッチ |
| CleaningOverride | 人数の手動上書き | 人 |
| PrevReservations / DisappearedRes | キャンセル検知用 | バッチ |

別ファイル：**Check-In Form（回答）** ＝ 宿泊者名簿。スプレッドシートID
`1IXVZLzJwJeaBG9Zi8xA6P32zK9L5E9h5DVC3ag3qsco` のシート `Form_Responses`。

---

## 3. CleaningBoard の列（最重要）

1行 ＝ 「ある日の、ある部屋」。

| 列 | 名前 | 意味 |
|---|---|---|
| **A** | 清掃 | **掃除の担当者名**（手入力） |
| **B** | 種類 | 入替 / リネン / 特別 / なし（手入力） |
| **C** | べ | セット人数＝布団の数（手入力） |
| **D** | 接客 | **接客の担当者名**（手入力） |
| **E** | キー | `2026-09-11_1F`。**一意** |
| F | 泊人 | その夜の宿泊人数 |
| G | 状態 | OUT→IN / OUT→空室 / 連泊 / IN / 空室 |
| **H** | 日付 | **日付での絞り込みはこの列を使う**（E列は文字列） |
| I | 曜日 |  |
| J | 部屋 | 1F / 2F |
| K | 人数ソース | 手動 / Lodgify / フォーム / 食事推定 / 不明 |
| **L** | 宿泊者名 |  |
| M | 予約元 | Booking.com の予約番号、または `yasuo.lodgify.com`（直予約） |
| N / O | 本日IN / 本日OUT |  |
| P | 次回IN日 |  |
| Q | 泊数 |  |
| **R** | 食事 | `Cold Shabu-Shabu(2人前), Ochazuke Breakfast(2人前)` のように1セルに詰まっている |
| S | 備考 | 「直予約」「⚠人数不明」など |
| T | 更新日時 |  |
| U | 清掃達成率 | 手入力 |
| V | 清掃やり直した箇所 | 手入力 |

## 4. 特シート

| 列 | 名前 |
|---|---|
| A | 対応者 |
| B | 完了日 |
| C | pt（作業ポイント） |
| D | 特別清掃（内容） |

**未完了 ＝ A と B がどちらも空の行。** 18件中12件が未完了（2026-09時点）。

---

## 5. スタッフの3つの質問と、答え方

担当者名は **A列・D列の値と完全一致**で探す。実在するのは次の6名。

`ゆか` `ゆうｻﾝ` `松原ｻﾝ` `まるこ` `や` `ななみ`
（`ゆうｻﾝ` `松原ｻﾝ` の「ｻﾝ」は**半角カナ**。後述）

### Q1.「今日の掃除、どんな内容だっけ？」

1. **A列 ＝ その人** かつ **H列 ＝ 今日** の行を拾う
2. **B列（種類）** と **C列（べ＝セット人数）** を答える。部屋は J列
3. **B列が「特別」なら**、特シートの**未完了**（A,B が空）の行を
   **C列（pt）と D列（内容）**つきで添える

### Q2.「今日の接客、誰を何階に案内する？食事は？」

1. **D列 ＝ その人** かつ **H列 ＝ 今日** の行を拾う
2. **J列（部屋）・G列（状態）・F列（泊人）・L列（宿泊者名）・R列（食事）** を答える
3. R列は1セルに詰まっているので、カンマで区切って1品ずつ読み上げると分かりやすい

### Q3.「今月、私は何日掃除で何日接客だった？」

1. **A列 ＝ その人 または D列 ＝ その人** かつ H列が当月の行を拾う
2. **日付の重複を除いて**数える（同じ日に1Fと2Fの両方を担当することがあるため、
   行数＝日数ではない）
3. 明細は B・C・F・G・L・R 列

例（実データ・まるこ さん / 2026年9月）：掃除 **7日** ／ 接客 **8日**。
行数はそれぞれ 7行・8行だが、9/11 は 1F と 2F の両方を掃除していて2行で1日。

---

## 6. 落とし穴（ここを外すと間違った答えを出す）

### ★タイムゾーンが食い違っている
スクリプトは **Asia/Tokyo**、スプレッドシートは **America/Los_Angeles**。
日付セルを素で読むと**1日ずれる**ことがある。
既存コードは必ず日本時間で `yyyy-MM-dd` に整形してから比較している。
シートをそのまま読むツールでも、日付は**表示されている文字列**を信じること。

### ★担当者名を正規化してはいけない
突合は完全一致。`ゆうｻﾝ` を `ゆうサン` に直すと**1件も拾えなくなる**。
実データを調べたところ表記ゆれは1件も無く、常に同じ表記なので、そのまま使えばよい。

### ★「-」は担当なし
空欄と「-」が混在している。どちらも担当者なしとして除外する。

### ★フォームは2種類ある。混同しないこと
| | 中身 | 場所 |
|---|---|---|
| FormResponses → LatestOptions | **食事の注文**・泉屋送迎・荷物・タクシー | 同じブック |
| Check-In Form | **宿泊者名簿**（氏名・住所・職業・電話） | **別スプレッドシート** |

実際にこれを取り違えて、一度バグを作り込んでいる。

### ★食事の注文経路は2つある（v2.14 から）
| 経路 | 注文のされ方 | LatestOptions での見分け方 |
|---|---|---|
| GoogleForm | 予約後にフォームで注文 | J列（その他要望）が空、K列に設問文のJSON |
| **Lodgify 予約時オプション** | 予約時のチェックアウト画面で購入 | **J列に `Lodgify予約時オプション`**、K列に `"_source":"lodgify"` |

どちらも同じ表・同じ書式（`Chicken Hot Pot(3人前)`）で入る。
**同じ客が両方使うことがある**（Lodgifyで夕食・フォームで朝食）。
その場合 LatestOptions には**生きている行が2行**でき、
CleaningBoard の食事列には `A / B` のように両方が出る。
片方を「再提出だから古い」と解釈してはいけない。

### ★CleaningBoard の E〜T列は毎時上書きされる
バッチが全消しして書き直す。**人が書いてよいのは A〜D 列と U・V 列だけ。**
E〜T列に手で書いても消える。

### ★行の位置は動かさない
開始日が `2026-08-01` に固定されていて、行は下に伸びるだけ。
並べ替えたり行を挿入したりすると、A〜D列の手入力が別の日にひもづく。

### ★直予約は iCal に出ない
Lodgify の直予約（M列が `yasuo.lodgify.com`）は Booking.com / Airbnb の
iCal に現れない。Lodgify API からしか取れない。S列に「直予約」と出る。

---

## 7. 自動処理（毎時）

`hourlySync` → `runBatch` が1時間ごとに、この順で動く。

1. iCal 取得（Booking.com / Airbnb）→ LatestReservations、消えた予約を検知
2. **Lodgify API 取得** → LodgifyBookings（人数と直予約の唯一の出どころ）
3. フォーム同期 → LatestOptions
   （**Lodgify 予約時オプションもここで同じ表に入る** → 人数の空欄を Lodgify から補完）
4. 担当者一覧 → Staff に追記
5. **CleaningBoard 生成**（iCal の骨格に Lodgify 直予約を合流 → 人数・氏名・食事を載せる）

失敗しても止まらないよう、2・4・5 は個別に握りつぶしてログに残す。
エラーが続く場合はオーナーにメール通知。

---

## 8. コードの場所

GitHub **`yasuhiro-fukuta/kashiwayasheet`**（`main` ブランチ）。

- `gas/` に Apps Script のソース（`.gs`）
- **`main` に push すると GitHub Actions が自動で Apps Script に反映**する
- 設定・手順は [`gas/README.md`](gas/README.md) と [`README.md`](README.md)

確認用の関数（**どれもシートに書き込まない**）：

| 関数 | 用途 |
|---|---|
| `selfTest()` | 全体の自己診断。NG 0 なら正常 |
| `diagnoseLodgifyMatch()` | 直予約・人数の突合がおかしいとき |
| `dumpCheckinForm()` | 宿泊者名簿フォームが読めているか |
| `explainRedKeys()` | E列の赤字の出どころ |
| `listPendingCheckinForms()` | 宿泊者名簿の未提出者 |
| `dumpLodgifyAddons()` | Lodgify 予約時オプションが API のどこに入っているか |

---

## 9. 残っている課題

- **スタッフ向け参照ページが未完成。** 画面設計と AppSheet 手順は作ってあるが、
  AppSheet は**閲覧のみでも1人1ライセンス**（7名で月 $35〜70）かかると分かり、
  Apps Script のウェブアプリで作り直そうとして中断した。作りかけは破棄済み。
  → 新スレッドでは、まず**チャットで質問に答える運用**で回す方針。
- `2026-10-14 1F` の人数が不明。Airbnb 経由で Lodgify に入らないため、
  `CleaningOverride` に手で1行足すしかない。
- `CleaningBoard` の可読性（列が22本あり横スクロールが必要）。これが今回の発端。
- **Lodgify 予約時オプションのフィールド名が実レスポンス未確認。**
  公開ドキュメントにアドオンの項目が無いため、コードは
  「既知のキー名 → 無ければ食事名で再帰走査」の2段構えにしてある。
  Apps Script で `dumpLodgifyAddons()` を1回実行し、ログの
  「取り出し経路」が `SCAN:` なら、出ているパス名を
  `CONFIG.LODGIFY.ADDONS.KEYS` に足すこと。0件なら
  リスト取得のレスポンスにアドオンが入っていないので Lodgify サポートに確認。
  詳細は [`gas/README.md`](gas/README.md) の「Lodgify 予約時オプション」。

---

## 10. 新スレッドへの依頼文（この下をそのまま使ってよい）

> 添付のスプレッドシート `kashiwaya_master_v2` は旅館の清掃・接客の予定表です。
> 上の引き継ぎ文書の §3〜§6 を前提に、スタッフからの次の質問に答えてください。
>
> - 「今日の掃除、どんな内容だっけ？」
> - 「今日の接客、誰を何階に案内する？食事は？」
> - 「今月、私は何日掃除で何日接客だった？」
>
> 担当者名は A列・D列の値と**完全一致**で探すこと（`ゆうｻﾝ` の「ｻﾝ」は半角カナ。直さない）。
> 日付の絞り込みは H列を使うこと。
> 同じ日に複数の部屋を担当することがあるので、**日数を数えるときは日付の重複を除く**こと。
